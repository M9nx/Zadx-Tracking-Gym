# SMTP Configuration for Gym Management System
# Gmail SMTP Settings

# Enable email sending
smtp.enabled=true
smtp.dryrun=false

# Gmail SMTP Configuration (Using SSL on port 465)
smtp.host=smtp.gmail.com
smtp.port=465
smtp.auth=true
smtp.starttls=false
smtp.ssl=true

# Gmail Account Credentials
smtp.username=gym.zadx@gmail.com
smtp.password=nylmcudjxzksxedy
smtp.from=gym.zadx@gmail.com

# Dry-run settings (used when dryrun=true)
smtp.dryrun.path=logs/emails/
